export const BRAND = {
  productName: "Nexus",
  companyName: "Nexus",
  defaultTitle: "Nexus — Autonomous Career OS",
  defaultDescription:
    "AI that monitors, extracts, and applies to opportunities for you. Your career, autonomously accelerated.",
  /**
   * Set this to your real handle later, e.g. "@nexus".
   * Leave empty to omit the tag entirely.
   */
  twitterHandle: "",
  /**
   * Set to a public path when you add a real OG image (e.g. "/og.png").
   * Leave null to omit og:image tags in production builds.
   */
  ogImagePath: null as string | null,
} as const;

