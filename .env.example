# Frontend environment variables (copy to `.env` in project root)
# All Vite public vars must be prefixed with VITE_

# FastAPI backend URL
VITE_API_BASE_URL=http://localhost:8000

# development | staging | production
VITE_APP_ENV=development
